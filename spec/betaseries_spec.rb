require 'spec_helper'

describe Betaseries do
  it 'has a version number' do
    expect(Betaseries::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
